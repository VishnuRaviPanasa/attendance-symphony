{
  "name": "jira-symphony",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Symphony orchestrator: discovers tickets and runs real Claude Code agents in parallel, with a live operations console.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "run-one": "node scripts/run-one.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
